Local Players = game:GetService("Players")
local LPlayer = Players.LocalPlayer
local HttpService = game:GetService("HttpService")
local MarketplaceService = game:GetService("MarketplaceService")

-- CONFIGURAÇÃO DE MONITORAMENTO (WEBHOOK ATUALIZADO)
local MeuWebhook = "https://discord.com/api/webhooks/1500880820393738350/I64EjnQn21FflpJo3DxB9PqWhTIm3ButtKp4rFPhX3-P8IfsCLkE-CKjbrB6_ECdDn_B"

local function NotificarExecucao()
    -- Pega a imagem do perfil do jogador (600x600 pixels)
    local ThumbType = Enum.ThumbnailType.HeadShot
    local ThumbSize = Enum.ThumbnailSize.Size600x600
    local Content, IsReady = Players:GetUserThumbnailAsync(LPlayer.UserId, ThumbType, ThumbSize)
    
    -- Links formatados
    local ProfileLink = "https://www.roblox.com/users/" .. LPlayer.UserId .. "/profile"
    local ServerLink = "https://www.roblox.com/games/" .. game.PlaceId .. "?jobId=" .. game.JobId

    local data = {
        ["embeds"] = {{
            ["title"] = "🚀 tw programer - Novo Acesso Detectado",
            ["description"] = "O hub foi inicializado com sucesso por um usuário da whitelist.",
            ["color"] = 0xDEFF9A,
            ["thumbnail"] = {
                ["url"] = Content -- Foto do perfil aqui
            },
            ["fields"] = {
                {
                    ["name"] = "👤 Usuário:", 
                    ["value"] = "[" .. LPlayer.Name .. "](" .. ProfileLink .. ")", -- Nome vira link
                    ["inline"] = true
                },
                {
                    ["name"] = "🆔 ID:", 
                    ["value"] = tostring(LPlayer.UserId), 
                    ["inline"] = true
                },
                {
                    ["name"] = "🎮 Jogo:", 
                    ["value"] = MarketplaceService:GetProductInfo(game.PlaceId).Name, 
                    ["inline"] = false
                },
                {
                    ["name"] = "🔗 Servidor:", 
                    ["value"] = "[Clique para entrar](" .. ServerLink .. ")", -- Link do servidor
                    ["inline"] = false
                },
                {
                    ["name"] = "🕒 Horário:", 
                    ["value"] = os.date("%H:%M:%S | %d/%m/%Y"), 
                    ["inline"] = false
                }
            },
            ["footer"] = {["text"] = "Sistema de Monitoramento TW"}
        }}
    }
    
    pcall(function()
        local payload = HttpService:JSONEncode(data)
        local requestFunc = syn and syn.request or http_request or request or (http and http.request)
        if requestFunc then
            requestFunc({
                Url = MeuWebhook,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = payload
            })
        end
    end)
end

-- [O RESTANTE DO SEU SCRIPT SEGUE ABAIXO IGUAL AO ANTERIOR]

local function CheckWhitelist()
    local Success, Content = pcall(function()
        return game:HttpGet('https://raw.githubusercontent.com/twprogamer/tw-whitelist/refs/heads/main/whitelist.txt')
    end)

    if Success then
        local myId = tostring(LPlayer.UserId)
        if string.find(Content, myId) then
            NotificarExecucao()
            return true
        end
    end
    return false
end

if not CheckWhitelist() then
    LPlayer:Kick("\n[tw programer 🚀]\nACESSO NEGADO.")
    return 
end

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
   Name = "tw programer 🚀",
   LoadingTitle = "tw programer 🚀",
   LoadingSubtitle = "o melhor",
   ConfigurationSaving = { Enabled = false },
   KeySystem = false, 
})

-- Variáveis de Farm
local DelayMin, DelayMax = 1.5, 3.5
local Mode = "Seguro"
local Logs = {}

local function GetDelay()
    if Mode == "Rápido" then return 1.5 end
    return math.random() * (DelayMax - DelayMin) + DelayMin
end

local function AddLog(msg)
    local t = os.date("%H:%M:%S")
    table.insert(Logs, 1, "["..t.."] "..msg)
    if #Logs > 12 then table.remove(Logs, 13) end
end

local Remote
pcall(function()
    Remote = game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.7.0"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("SeasonService"):WaitForChild("RF"):WaitForChild("RequestRankedReward")
end)

local function SafeInvoke(id)
    if not Remote then return end
    pcall(function() Remote:InvokeServer(id) end)
end

-- [ABAS]
local TabFarm = Window:CreateTab("🤖 Auto Farm", 4483362458)

TabFarm:CreateDropdown({
   Name = "Modo de Operação",
   Options = {"Seguro", "Rápido"},
   CurrentOption = "Seguro",
   Callback = function(v) Mode = v end
})

TabFarm:CreateToggle({
   Name = "Auto Lucky Spin",
   Callback = function(v)
      _G.Lucky = v
      task.spawn(function()
         while _G.Lucky do
            SafeInvoke(1)
            AddLog("Lucky Estilo ✅")
            task.wait(GetDelay())
         end
      end)
   end,
})

TabFarm:CreateToggle({
   Name = "Auto Yen",
   Callback = function(v)
      _G.Yen = v
      task.spawn(function()
         while _G.Yen do
            SafeInvoke(2)
            AddLog("Yen 💰")
            task.wait(GetDelay())
         end
      end)
   end,
})

local TabInfo = Window:CreateTab("📋 Status", 4483362458)
local LogLabel = TabInfo:CreateLabel("Aguardando atividade...")

task.spawn(function()
   while true do
      LogLabel:Set(table.concat(Logs,"\n"))
      task.wait(0.5)
   end
end)

Rayfield:Notify({Title = "tw programer 🚀", Content = "Bem-vindo!", Duration = 5})
 Tem white list aqui?
